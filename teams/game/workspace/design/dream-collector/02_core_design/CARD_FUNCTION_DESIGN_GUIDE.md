# 🃏 Dream Collector - 카드 기능 설계 가이드 v2.1

**문서 목적**: '카드 기능 생성 매트릭스'를 기반으로, 게임에 구현될 카드의 모든 기능을 유형별로 분류하고 구체적인 예시를 제시하여 개발팀이 참조할 수 있는 기획 문서를 제공합니다.

---

## 1. 카드 효과 생성 매트릭스 (v2.0 요약)

모든 카드는 `[비용/조건] → [결과]` 구조를 따르며, '결과'는 `[대상]의 [자원]을(를) [값]만큼 [행동]`하는 하나 이상의 **'카드 효과 원자(Atom)'** 로 구성됩니다.

### 🧩 매트릭스 구성 요소

| 요소 | 종류 (예시) |
| :--- | :--- |
| **① 대상 (Target)** | 나, 단일 적, 모든 적, 무작위 적, 손패, 덱, 버림패, 소멸패... (10+) |
| **② 자원 (Resource)** | HP, 방어도, 에너지, 힘, 민첩, 악몽, 취약, 카드, 상태이상... (15+) |
| **③ 행동 (Action)** | 얻는다, 잃는다, 준다, 부여한다, 뽑는다, 버린다, 소멸시킨다... (10+) |
| **④ 값 (Value)** | 소(S), 중(M), 대(L), 특대(XL), 변수(X) (5 등급) |

---

## 2. 생성 가능한 카드 기능 목록 (카테고리별 분류)

이 목록은 매트릭스의 조합으로 생성된 구체적인 카드 기능들을 6가지 핵심 카테고리로 분류한 것입니다. 개발팀은 이 기능들을 조합하여 실제 카드를 구현할 수 있습니다. `[값]`은 밸런싱 단계에서 구체적인 수치로 변환됩니다.

### ⚔️ ① 공격형 기능 (Offensive)

| 기능 ID | 비용/조건 | 결과 (효과 원자) | 설명 | 원형 |
| :--- | :--- | :--- | :--- | :--- |
| ATK-01 | (없음) | `[단일 적]에게 [HP]를 [값]만큼 [준다]` | 기본 공격 | 공용 |
| ATK-02 | (없음) | `[모든 적]에게 [HP]를 [값]만큼 [준다]` | 광역 공격 | 공용 |
| ATK-03 | (없음) | `[무작위 적]에게 [HP]를 [값]만큼 [준다]` (N회 반복) | 무작위 다중 타격 | 꿈의 분노 |
| ATK-04 | `[내]가 [HP]를 [값]만큼 [잃는다]` | `[단일 적]에게 [HP]를 [값 XL]만큼 [준다]` | 고위험 고수익 공격 | 꿈의 분노 |
| ATK-05 | (없음) | `[내]가 가진 [힘]만큼 ` + `[단일 적]에게 [HP]를 [준다]` | 힘 비례 공격 | 꿈의 분노 |
| ATK-06 | (없음) | `[적]이 가진 [악몽] 중첩만큼 ` + `[HP]를 [준다]` | 악몽 비례 공격 | 뒤틀린 악몽 |
| ATK-07 | (없음) | `[내]가 가진 [방어도]의 50%만큼 ` + `[모든 적]에게 [HP]를 [준다]` | 방어도 비례 공격 | 고요한 방벽 |
| ATK-08 | `[내]가 [에너지]를 모두 [잃는다]` | `잃은 [에너지] X [값]만큼 ` + `[단일 적]에게 [HP]를 [준다]` | 에너지 소모 공격 | 샘솟는 영감|
| ATK-09 | (없음) | `[단일 적]에게 [HP]를 [값]만큼 [준다]` + `[취약]을 [값]만큼 [부여한다]`| 공격 + 디버프 | 뒤틀린 악몽 |

---

### 🛡️ ② 방어형 기능 (Defensive)

| 기능 ID | 비용/조건 | 결과 (효과 원자) | 설명 | 원형 |
| :--- | :--- | :--- | :--- | :--- |
| DEF-01 | (없음) | `[나]에게 [방어도]를 [값]만큼 [얻는다]` | 기본 방어 | 공용 |
| DEF-02 | (없음) | `[나]에게 [방어도]를 [값]만큼 [얻는다]` + `[가시]를 [값]만큼 [얻는다]` | 방어 + 반격 | 고요한 방벽 |
| DEF-03 | `[손패]의 [카드] 1장을 [버린다]` | `[나]에게 [방어도]를 [값 L]만큼 [얻는다]` | 카드 소모 방어 | 샘솟는 영감|
| DEF-04 | (없음) | `이번 턴에 얻은 [방어도]는 사라지지 않는다` (지속 효과) | 방어도 유지(파워) | 고요한 방벽 |
| DEF-05 | (없음) | `[나]의 [민첩]을 [값]만큼 [얻는다]` | 방어 능력 강화(파워)| 고요한 방벽 |
| DEF-06 | (없음) | `피격 시 [카드]를 1장 [뽑는다]` (지속 효과) | 피격 시 드로우(파워)| 샘솟는 영감|
| DEF-07 | (없음) | `덱에서 [상태이상 카드] 1장을 [소멸시킨다]` | 덱 정화 | 고요한 방벽 |
| DEF-08 | `[내]가 [HP]를 [값]만큼 [회복한다]` | (없음) | 생명력 회복 | 공용 |

---

### 💪 ③ 강화 기능 (Buff / Scaling)

| 기능 ID | 비용/조건 | 결과 (효과 원자) | 설명 | 원형 |
| :--- | :--- | :--- | :--- | :--- |
| BUF-01 | (없음) | `[나]의 [힘]을 [값]만큼 [얻는다]` | 영구 힘 증가 (파워) | 꿈의 분노 |
| BUF-02 | (없음) | `이번 턴에만 ` + `[나]의 [힘]을 [값]만큼 [얻는다]` | 순간 힘 증가 | 꿈의 분노 |
| BUF-03 | `[덱]에 [상태이상 카드]를 [섞어 넣는다]` | `[나]의 [힘]을 [값 L]만큼 [얻는다]` | 대가가 있는 힘 | 뒤틀린 악몽 |
| BUF-04 | (없음) | `[나]의 [가시]를 [값]만큼 [얻는다]` | 반격 데미지 증가 | 고요한 방벽 |
| BUF-05 | (없음) | `매 턴 시작 시 [에너지]를 1 [얻는다]` (지속 효과) | 에너지 수급 (파워) | 샘솟는 영감|
| BUF-06 | (없음) | `매 턴 시작 시 [카드]를 1장 더 [뽑는다]` (지속 효과) | 드로우 강화 (파워) | 샘솟는 영감|
| BUF-07 | `카드를 낼 때마다` | `[나]의 [힘]을 1 [얻는다]` (지속 효과) | 카드 사용 비례 강화| 샘솟는 영감|
| BUF-08 | `적을 처치할 때마다` | `[나]의 최대 [HP]를 1 [얻는다]` (지속 효과)| 성장 | 꿈의 분노 |

---

### 💀 ④ 약화 기능 (Debuff / Curses)

| 기능 ID | 비용/조건 | 결과 (효과 원자) | 설명 | 원형 |
| :--- | :--- | :--- | :--- | :--- |
| DEB-01 | (없음) | `[단일 적]에게 [취약]을 [값]만큼 [부여한다]` | 단일 취약 | 뒤틀린 악몽 |
| DEB-02 | (없음) | `[모든 적]에게 [약화]를 [값]만큼 [부여한다]` | 광역 약화 | 뒤틀린 악몽 |
| DEB-03 | (없음) | `[단일 적]에게 [악몽]을 [값]만큼 [부여한다]` | 중독 부여 | 뒤틀린 악몽 |
| DEB-04 | `[적]이 [악몽] 상태라면` | `추가로 [악몽]을 [값]만큼 [부여한다]` | 조건부 중독 심화 | 뒤틀린 악몽 |
| DEB-05 | (없음) | `[적]의 [힘]을 [값]만큼 [잃게 한다]` | 공격력 감소 | 고요한 방벽 |
| DEB-06 | (없음) | `[적]의 [모든 버프]를 [제거한다]` | 버프 제거 | 고요한 방벽 |
| DEB-07 | (없음) | `[적]의 [덱]에 [상태이상 카드]를 [섞어 넣는다]` | 덱 오염 (보스 전용)| 뒤틀린 악몽 |

---

### 🌀 ⑤ 카드 조작 기능 (Manipulation)

| 기능 ID | 비용/조건 | 결과 (효과 원자) | 설명 | 원형 |
| :--- | :--- | :--- | :--- | :--- |
| MAN-01 | (없음) | `[덱]에서 [카드]를 [값]만큼 [뽑는다]` | 기본 드로우 | 공용 |
| MAN-02 | `[에너지]가 0이라면` | `[카드]를 1장 [뽑는다]` | 조건부 드로우 | 샘솟는 영감|
| MAN-03 | (없음) | `[버림패]에서 [카드] 1장을 골라 [가져온다]` | 선택적 회수 | 샘솟는 영감|
| MAN-04 | (없음) | `[소멸패]에서 [카드] 1장을 [가져온다]` | 소멸된 카드 회수 | 샘솟는 영감|
| MAN-05 | (없음) | `[손패]에서 [카드] N장을 [버리고]`, `N+1장을 [뽑는다]` | 손패 교체 | 공용 |
| MAN-06 | (없음) | `[손패]에서 [공격 카드] 1장을 골라 비용을 0으로 [설정한다]`| 비용 조작 | 샘솟는 영감|
| MAN-07 | (없음) | `[손패]에서 [카드] 1장을 [소멸시킨다]` + `[에너지] 2를 [얻는다]`| 카드 소모 자원 수급| 뒤틀린 악몽 |
| MAN-08 | (없음) | `[덱]을 보고 카드 3장을 골라 순서를 정한 뒤 맨 위에 놓는다`| 덱 순서 조작 | 샘솟는 영감|

---

### ✨ ⑥ 특수/복합 기능 (Special / Hybrid)

| 기능 ID | 비용/조건 | 결과 (효과 원자) | 설명 | 원형 |
| :--- | :--- | :--- | :--- | :--- |
| SPC-01 | (없음) | `이번 턴에 사용하는 다음 카드를 한 번 더 발동시킨다` | 더블탭/에코폼 | 샘솟는 영감|
| SPC-02 | (없음) | `[나]의 [방어도]를 모두 [잃는다]` + `잃은 [방어도]만큼 [모든 적]에게 [피해]를 [준다]`| 방어도 폭발 | 고요한 방벽 |
| SPC-03 | (없음) | `이번 턴에 받는 모든 피해는 다음 턴에 받는다` | 피해 지연 | 고요한 방벽 |
| SPC-04 | (없음) | `[적]의 다음 행동을 미리 본다` | 예지 | 공용 |
| SPC-05 | `[나]의 [HP]가 50% 이하라면` | `비용이 0이 된다` | 조건부 비용 0 | 꿈의 분노 |
| SPC-06 | `이번 턴에 카드를 5장 이상 사용했다면`| `[카드]를 2장 [뽑는다]` | 콤보 보상 | 뒤틀린 악몽 |
| SPC-07 | (없음) | `무작위 [파워 카드] 1장을 생성하여 발동시킨다` | 무작위성 | 샘솟는 영감|

---

## 4. 개발 반영을 위한 다음 단계

### 1. **JSON 스키마 설계**
위 기능 ID와 매트릭스를 기반으로 `cards.json`에 들어갈 구체적인 스키마를 설계합니다.

**예시 `effects` 필드 구조**:
```json
"effects": [
  {
    "action": "DEAL",
    "target": "E1",
    "resource": "HP",
    "value": "M",
    "value_concrete": 6
  },
  {
    "action": "APLY",
    "target": "E1",
    "resource": "VUL",
    "value": "M",
    "value_concrete": 1
  }
]
```

### 2. **CardEffectSystem 구현**
`CardEffectSystem.gd`에서 위 기능 ID 또는 `action`, `target`, `resource` 조합에 따른 실제 게임 로직을 구현하는 `switch` 또는 `match` 문을 작성합니다.

### 3. **카드 리스트 작성 (스프레드시트)**
개발과 동시에, 기획자는 위 기능 목록을 참고하여 4대 원형과 등급에 맞춰 실제 카드 이름과 스토리를 부여하는 작업을 진행합니다.

**스프레드시트 컬럼**:
`카드 이름`, `원형`, `기능 ID (조합)`, `비용`, `효과 설명`, `레퍼런스/스토리`

---
_Last updated: 2026-02-27 by Atlas_
